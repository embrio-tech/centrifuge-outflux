#import JSON from 'scalars.graphql'

type Aggregations {
  """
  fico weighted average by normilized debt
  """
  ficoWeightedByNormalizedDebt: [JSON!]

  """
  cummulated normalized debt per state
  """
  sumOfNormalizedDebtPerState: [JSON!]
}
